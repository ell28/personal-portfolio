// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set this to your deployed URL (used for canonical links, sitemaps, etc.)
  site: 'https://elliezer.dev',
});
